// Your Tuval config. This file is yours: boot imports it and registers every program row in the
// list it default-exports. Empty until the registry slice defines what a row is.
const programs: ReadonlyArray<unknown> = [];

export default programs;
