import "./Popover.css";

/**
 * @component Popover
 * @whenToUse The Manti-backed non-modal anchored surface for compact interactive
 *   content such as the notification preview. Use Dialog when the task must be modal.
 * @slot trigger The element that anchors and opens the popover.
 * @slot children The popover content.
 */
export type {PopoverProps} from "@manti-ui/react";
export {Popover} from "@manti-ui/react";
