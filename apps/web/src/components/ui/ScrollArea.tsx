/**
 * @component ScrollArea
 * @whenToUse A bounded Manti-backed scroll viewport that needs keyboard focus
 *   and draggable scrollbars while its surrounding header/footer stay fixed.
 * @slot children Scrollable content.
 */
export type {ScrollAreaProps} from "@manti-ui/react";
export {ScrollArea} from "@manti-ui/react";
