/**
 * @component NumberInput
 * @whenToUse A Manti-backed numeric field with bounds and step controls. Prefer
 *   this over a generic Input with `type="number"` when numeric stepping is the
 *   interaction.
 * @slot label Optional visible field label.
 */
export type {NumberInputProps} from "@manti-ui/react";
export {NumberInput} from "@manti-ui/react";
