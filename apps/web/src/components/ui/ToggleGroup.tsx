import "./ToggleGroup.css";

/**
 * @component ToggleGroup
 * @whenToUse The Manti-backed compact selection set for segmented controls,
 *   filters and small fixed option lists. Supply options through `items` and
 *   control selection with the string-array `value` contract.
 * @slot items Toggle options supplied through `items`.
 */
export type {ToggleGroupItem, ToggleGroupProps} from "@manti-ui/react";
export {ToggleGroup} from "@manti-ui/react";
