/**
 * @component Collapsible
 * @whenToUse The Manti-backed inline disclosure. Pass the trigger and panel
 *   content to reveal optional detail while retaining keyboard and ARIA behavior.
 * @slot trigger The disclosure control supplied through the `trigger` prop.
 * @slot children The region shown while the disclosure is open.
 */
export type {CollapsibleProps} from "@manti-ui/react";
export {Collapsible} from "@manti-ui/react";
