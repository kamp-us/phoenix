/**
 * @component Badge
 * @whenToUse A Manti-backed compact status or category chip. Keep state in text,
 *   not color alone, and use a feature class only to preserve an established
 *   Phoenix category palette.
 * @slot children The badge label.
 */
export type {BadgeProps, BadgeVariant} from "@manti-ui/react";
export {Badge} from "@manti-ui/react";
