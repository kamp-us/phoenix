# Patterns

An index with no docs registered yet — the fixture behind `pattern corpus`'s `none` outcome.

## When to add a new pattern doc here

Add a doc when the shape is used in 2+ places, is non-obvious from reading the code, and a future
agent would otherwise invent a worse version.
