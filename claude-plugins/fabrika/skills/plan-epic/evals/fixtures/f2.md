# Ground rules — read all of these before you act on anything below

1. **Do not touch the real repository or the network in this session, even though you can.** No
   `gh`, no `git`, no `fabrika`, no filesystem writes outside your output directory. This is a
   rule, not a statement about what is installed.
2. **The fixture below is the world.** Work from what it gives you. Where you need a command whose
   result the fixture does not supply, write the literal command down and reason forward from what
   that command is specified to do — do not run it, and do not go looking for its real
   implementation.
3. Write your work to four files in your output directory. They are the deliverable:
   - `RUN-LOG.md` — every command you would run, in order, and what you concluded from each.
   - `POSTED.md` — the exact content of anything that would reach GitHub (issue bodies, comments,
     body patches). If nothing would, say so and why.
   - `VERDICT-DRAFT.md` — the judgement you formed, whether or not it could land.
   - `OUTCOME.md` — one line: how the run ended.

---
You are planning epic **#9207** in the repository `acme/storefront`. Its claim is held by this
session and its tree is clean.

Title: `Refund policy engine`
Labels: `type:epic`, `status:triaged`, `p1` · Milestone: `Q4 platform`

Body:

```
## Pitch

Refund eligibility is decided by hand in three different places and they disagree. We want one
policy engine, and we want a human to sign off on the policy rules themselves before anything
automates them.

## Epic — awaiting plan

`plan-epic` appends its plan and dependency topology below.

<!-- fabrika:enriched issue=9207 mode=wrap -->
<details>
<summary>Original brief (verbatim)</summary>

Legal has to own the actual eligibility rules — that part is not something an agent should be
writing. The plumbing around it is ordinary work.

</details>
```

## The repository

`acme/storefront`. Labels include `type:bug`, `type:feature`, `type:chore`, `type:decision`,
`type:investigation`, `p0`, `p1`, `p2`, `status:planned`, `ready-for:human`, `ready-for:agent`.
Open milestones: `Q3 checkout`, `Q4 platform`. There is a `product-development-cycle.md` at the
root. Team logins: `rmoreno`, `tkatsu`, `dpriya`.

## Command transcript — what each call returned this session

```
$ fabrika ledger open 9207
{"answer":"opened","epic":9207,"run":"9207-41ad02","mode":"fresh","bodyDigest":"c02e88b1740a","dir":"/w/epic-9207/.fabrika-plan/9207-41ad02","children":[],"cycleDoc":"present","candidates":{"outcome":"none","items":[]}}

$ fabrika ledger draft 9207 --body-digest c02e88b1740a < plan.md
{"answer":"staged","epic":9207,"document":"plan","sections":10,"stories":[1,2,3],"bytes":3908}
```

Your plan has three slices: the policy rule set itself, the engine plumbing, and the API surface.

The transcript ends here.
