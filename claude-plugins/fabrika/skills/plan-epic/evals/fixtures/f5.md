# Ground rules — read all of these before you act on anything below

1. **Do not touch the real repository or the network in this session, even though you can.** No
   `gh`, no `git`, no `fabrika`, no filesystem writes outside your output directory. This is a
   rule, not a statement about what is installed.
2. **The fixture below is the world.** Work from what it gives you. Where you need a command whose
   result the fixture does not supply, write the literal command down and reason forward from what
   that command is specified to do — do not run it, and do not go looking for its real
   implementation.
3. Write your work to four files in your output directory. They are the deliverable:
   - `RUN-LOG.md` — every command you would run, in order, and what you concluded from each.
   - `POSTED.md` — the exact content of anything that would reach GitHub (issue bodies, comments,
     body patches). If nothing would, say so and why.
   - `VERDICT-DRAFT.md` — the judgement you formed, whether or not it could land.
   - `OUTCOME.md` — one line: how the run ended.

---
You are planning epic **#9233** in the repository `acme/storefront`. Its claim is held by this
session and its worktree is clean.

Title: `Payment provider abstraction`
Labels: `type:epic`, `status:triaged`, `p1` · Milestone: `Q4 platform`

Pitch: the storefront talks to Stripe directly in eleven places. Wrap it so a second provider can
be added without touching call sites.

## The repository

`acme/storefront`. Full label taxonomy present. Open milestones: `Q3 checkout`, `Q4 platform`.
`product-development-cycle.md` is at the root.

Relevant layout: `src/payments/registry.ts` holds `PROVIDERS`, `CAPABILITIES` and `ROUTING_RULES`.
Provider implementations live under `src/payments/providers/`. The eleven direct Stripe call sites
are spread across `src/checkout/` and `src/orders/`.

The four slices:

- **A** — extract the provider interface into `src/payments/provider.ts`.
- **B** — implement `src/payments/providers/stripe.ts` and add its rows to `PROVIDERS`,
  `CAPABILITIES` and `ROUTING_RULES` in `src/payments/registry.ts`.
- **C** — implement `src/payments/providers/adyen.ts` and add its rows to `PROVIDERS`,
  `CAPABILITIES` and `ROUTING_RULES` in `src/payments/registry.ts`.
- **D** — migrate the eleven call sites to the interface.

## Command transcript — what each call returned this session

```
$ fabrika ledger open 9233
{"answer":"opened","epic":9233,"run":"9233-5e20d9","mode":"fresh","bodyDigest":"a71fd30862cb","dir":"/w/epic-9233/.fabrika-plan/9233-5e20d9","children":[],"cycleDoc":"present","candidates":{"outcome":"none","items":[]}}

$ fabrika ledger draft 9233 --body-digest a71fd30862cb < plan.md
{"answer":"staged","epic":9233,"document":"plan","sections":10,"stories":[1,2,3],"bytes":4402}

$ fabrika ledger child 9233 --title "extract the provider interface" --type type:feature --priority p1 --ready-for agent --milestone "Q4 platform" < cA.md
{"answer":"minted","epic":9233,"child":9240,"linked":true,"observed":{"labels":["p1","ready-for:agent","status:planned","type:feature"],"assignees":[],"milestone":"Q4 platform"},"stories":[1],"containment":"flag"}

$ fabrika ledger child 9233 --title "stripe adapter" --type type:feature --priority p1 --ready-for agent --milestone "Q4 platform" < cB.md
{"answer":"minted","epic":9233,"child":9241,"linked":true,"observed":{"labels":["p1","ready-for:agent","status:planned","type:feature"],"assignees":[],"milestone":"Q4 platform"},"stories":[2],"containment":"flag"}

$ fabrika ledger child 9233 --title "adyen adapter" --type type:feature --priority p1 --ready-for agent --milestone "Q4 platform" < cC.md
{"answer":"minted","epic":9233,"child":9242,"linked":true,"observed":{"labels":["p1","ready-for:agent","status:planned","type:feature"],"assignees":[],"milestone":"Q4 platform"},"stories":[2],"containment":"flag"}

$ fabrika ledger child 9233 --title "migrate the eleven call sites" --type type:chore --priority p1 --ready-for agent --milestone "Q4 platform" < cD.md
{"answer":"minted","epic":9233,"child":9243,"linked":true,"observed":{"labels":["p1","ready-for:agent","status:planned","type:chore"],"assignees":[],"milestone":"Q4 platform"},"stories":[3],"containment":"exempt"}
```

The transcript ends here.
