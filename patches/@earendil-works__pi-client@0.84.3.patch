diff --git a/dist/client.d.ts b/dist/client.d.ts
index b228250ed431353a1d2eee0fb634c3d4d21b8612..173ed6d76152c8557f1533a74f504d0d1ebef7ab 100644
--- a/dist/client.d.ts
+++ b/dist/client.d.ts
@@ -8,6 +8,7 @@ export declare class PiClient {
     get connectionState(): ConnectionState;
     get connected(): boolean;
     get snapshot(): ServerSnapshot | undefined;
+    get pendingRequestCount(): number;
     static connect(options: PiClientOptions): Promise<PiClient>;
     connect(): Promise<ServerSnapshot>;
     reconnect(): Promise<ServerSnapshot>;
@@ -16,8 +17,8 @@ export declare class PiClient {
     onEvent(listener: (event: ServerEvent) => void): Unsubscribe;
     onConnectionStateChange(listener: (change: ConnectionStateChange) => void): Unsubscribe;
     listSessions(): Promise<readonly SessionMetadata[]>;
-    createSession(options?: CreateSessionOptions): Promise<PiSessionHandle>;
-    attachSession(sessionId: string): Promise<PiSessionHandle>;
+    createSession(options?: CreateSessionOptions, signal?: AbortSignal): Promise<PiSessionHandle>;
+    attachSession(sessionId: string, signal?: AbortSignal): Promise<PiSessionHandle>;
     acquireSession(sessionId: string, options: AcquireSessionOptions): Promise<PiSessionHandle>;
     dispose(): Promise<void>;
     [Symbol.asyncDispose](): Promise<void>;
diff --git a/dist/client.js b/dist/client.js
index 11fe04e8e9db062abbfb66b49c1ff60182216dbe..656afdbf2e27fead982e38b88890f660cc830958 100644
--- a/dist/client.js
+++ b/dist/client.js
@@ -1,6 +1,6 @@
 import { encodeClientMessage, ProtocolValidationError, } from "@earendil-works/pi-protocol";
 import { Connection } from "./connection.js";
-import { PiClientDisposedError, PiDisconnectedError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, toError, } from "./errors.js";
+import { PiClientDisposedError, PiDisconnectedError, PiRequestCancelledError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, toError, } from "./errors.js";
 import { createPromiseResolvers } from "./promise.js";
 import { SessionHandle, } from "./session-handle.js";
 import { ClientState } from "./state.js";
@@ -18,6 +18,7 @@ export class PiClient {
     #sessionReconciliations = new Map();
     #connectionStateListeners = new Set();
     #requestSequence = 0;
+    #cancelledRequestSequence = 0;
     #disposed = false;
     #disposePromise;
     constructor(options) {
@@ -43,6 +44,9 @@ export class PiClient {
     get snapshot() {
         return this.#state.snapshot;
     }
+    get pendingRequestCount() {
+        return this.#pendingRequests.size;
+    }
     static async connect(options) {
         const client = new PiClient(options);
         try {
@@ -83,13 +87,13 @@ export class PiClient {
     async listSessions() {
         return (await this.#request({ command: "list" })).sessions;
     }
-    async createSession(options = {}) {
-        const result = await this.#request({ command: "create", ...options });
+    async createSession(options = {}, signal) {
+        const result = await this.#request({ command: "create", ...options }, signal);
         const token = this.#reserveSessionLease(result.session.id, "exclusive");
         return this.#createSessionLease(result.session.id, token);
     }
-    async attachSession(sessionId) {
-        return this.acquireSession(sessionId, { mode: "shared" });
+    async attachSession(sessionId, signal) {
+        return this.acquireSession(sessionId, { mode: "shared", signal });
     }
     async acquireSession(sessionId, options) {
         this.#assertNotDisposed();
@@ -104,7 +108,7 @@ export class PiClient {
             if (reconciled || !this.#state.isSessionAttached(sessionId)) {
                 let attachment = this.#sessionAttachments.get(sessionId);
                 if (!attachment) {
-                    attachment = this.#attachSession(sessionId);
+                    attachment = this.#attachSession(sessionId, options.signal);
                     this.#sessionAttachments.set(sessionId, attachment);
                 }
                 try {
@@ -122,10 +126,10 @@ export class PiClient {
             throw error;
         }
     }
-    async #attachSession(sessionId) {
+    async #attachSession(sessionId, signal) {
         const previous = this.#state.forgetSessionSnapshot(sessionId);
         try {
-            await this.#request({ command: "attach", sessionId });
+            await this.#request({ command: "attach", sessionId }, signal);
         }
         catch (error) {
             if (previous)
@@ -133,14 +137,33 @@ export class PiClient {
             throw error;
         }
     }
-    #request(command) {
+    #request(command, signal) {
         if (this.#disposed)
             return Promise.reject(new PiClientDisposedError());
         if (!this.connected)
             return Promise.reject(new PiDisconnectedError());
-        const id = `request-${++this.#requestSequence}`;
+        if (signal?.aborted)
+            return Promise.reject(new PiRequestCancelledError());
+        const requestSequence = ++this.#requestSequence;
+        const id = `request-${requestSequence}`;
         const { promise, resolve, reject } = createPromiseResolvers();
-        this.#pendingRequests.set(id, { command, resolve, reject });
+        const pending = { command, resolve, reject, unregisterAbort: () => { } };
+        this.#pendingRequests.set(id, pending);
+        if (signal) {
+            const onAbort = () => {
+                const cancelled = this.#takePendingRequest(id);
+                if (!cancelled)
+                    return;
+                this.#cancelledRequestSequence = Math.max(this.#cancelledRequestSequence, requestSequence);
+                cancelled.reject(new PiRequestCancelledError());
+            };
+            signal.addEventListener("abort", onAbort, { once: true });
+            pending.unregisterAbort = () => signal.removeEventListener("abort", onAbort);
+            if (signal.aborted)
+                onAbort();
+        }
+        if (!this.#pendingRequests.has(id))
+            return promise;
         let frame;
         try {
             frame = encodeClientMessage({ type: "request", id, request: command }, { maxFrameLength: this.#connection.maxFrameLength });
@@ -253,6 +276,9 @@ export class PiClient {
         }
         const pending = this.#takePendingRequest(message.id);
         if (!pending) {
+            const sequence = /^request-([0-9]+)$/.exec(message.id)?.[1];
+            if (sequence !== undefined && Number(sequence) <= this.#cancelledRequestSequence)
+                return;
             this.#connection.fail(new ProtocolValidationError("Response has no matching request"));
             return;
         }
@@ -279,15 +305,19 @@ export class PiClient {
     }
     #takePendingRequest(id) {
         const request = this.#pendingRequests.get(id);
-        if (request)
+        if (request) {
             this.#pendingRequests.delete(id);
+            request.unregisterAbort();
+        }
         return request;
     }
     #rejectPendingRequests(error) {
         const requests = [...this.#pendingRequests.values()];
         this.#pendingRequests.clear();
-        for (const request of requests)
+        for (const request of requests) {
+            request.unregisterAbort();
             request.reject(error);
+        }
     }
     dispose() {
         if (this.#disposePromise)
diff --git a/dist/errors.d.ts b/dist/errors.d.ts
index 72e26512732a4db4ee72c4ffa0f5f11c6694f65d..897e32108019f5dcc81ca4afe81fbd24cc515dbf 100644
--- a/dist/errors.d.ts
+++ b/dist/errors.d.ts
@@ -10,6 +10,9 @@ export declare class PiDisconnectedError extends Error {
 export declare class PiClientDisposedError extends Error {
     constructor();
 }
+export declare class PiRequestCancelledError extends Error {
+    constructor();
+}
 export declare class PiSessionOwnershipError extends Error {
     readonly sessionId: string;
     constructor(sessionId: string, message: string);
diff --git a/dist/errors.js b/dist/errors.js
index 3a351b6e952a7dce033a30ecafb0a7192891b7bd..e0b5ad23529d7b0004214b9f8ff86083fd4b0ae8 100644
--- a/dist/errors.js
+++ b/dist/errors.js
@@ -20,6 +20,12 @@ export class PiClientDisposedError extends Error {
         this.name = "PiClientDisposedError";
     }
 }
+export class PiRequestCancelledError extends Error {
+    constructor() {
+        super("Pi client request was cancelled");
+        this.name = "PiRequestCancelledError";
+    }
+}
 export class PiSessionOwnershipError extends Error {
     sessionId;
     constructor(sessionId, message) {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index 00b0f07dffb46046e12b4224814979a0dadde3eb..c55fe8898a6b029ae139fc0c9fc8d9b80f1e0c4f 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1,5 +1,5 @@
 export { PiClient } from "./client.ts";
-export { PiClientDisposedError, PiDisconnectedError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, } from "./errors.ts";
+export { PiClientDisposedError, PiDisconnectedError, PiRequestCancelledError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, } from "./errors.ts";
 export type { AcquireSessionOptions, PiSessionHandle, SessionLease, SessionLeaseMode } from "./session-handle.ts";
 export type { ByteTransport, ByteTransportFactory, ByteTransportHandlers } from "./transport.ts";
 export type { ConnectionState, ConnectionStateChange, CreateSessionOptions, ListenerErrorHandler, PiClientOptions, Unsubscribe, } from "./types.ts";
diff --git a/dist/index.js b/dist/index.js
index 010ef02ebf389aa43d49bd54463751e0bddc83de..bd3ad93a8f34a03d221c6172da7d8c02a4b569ea 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,3 +1,3 @@
 export { PiClient } from "./client.js";
-export { PiClientDisposedError, PiDisconnectedError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, } from "./errors.js";
+export { PiClientDisposedError, PiDisconnectedError, PiRequestCancelledError, PiServerError, PiSessionDetachedError, PiSessionOwnershipError, } from "./errors.js";
 //# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/dist/session-handle.d.ts b/dist/session-handle.d.ts
index 38f3c5e44ae3f17035805ce39a7e74a3f08401ab..4d617713fd5ffec558d0a3fae793149ec816ed74 100644
--- a/dist/session-handle.d.ts
+++ b/dist/session-handle.d.ts
@@ -6,6 +6,7 @@ type SessionCommand = Extract<Command, {
 export type SessionLeaseMode = "shared" | "exclusive";
 export interface AcquireSessionOptions {
     mode: SessionLeaseMode;
+    signal?: AbortSignal;
 }
 export interface SessionLease extends AsyncDisposable {
     readonly id: string;
