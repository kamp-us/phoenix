# GitHub automation

Workflow shell must preserve command failures and cleanup status. Read the shell
guidance through the shared index before changing a `run:` block; the runner's
defaults can affect that status handling.

The shared [pattern index](../.patterns/index.md) routes workflow, shell and release
changes to their current guidance. Preserve workflow scope and required checks.
