{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "fabrika config (.fabrika.jsonc)",
	"description": "Every key fabrika reads from .fabrika.jsonc. Each is independently optional and falls to its shipped default when absent; this schema documents the shape of a declared value. Generated by `fabrika config schema --write` — edit the key fragments in packages/fabrika-cli/src/config/keys/, never this file.",
	"type": "object",
	"properties": {
		"$schema": {
			"type": "string",
			"description": "Path to the JSON Schema for this file — e.g. \"./.fabrika.schema.json\". Gives an editor autocomplete and validation for the keys below."
		},
		"boardVocabulary": {
			"type": "object",
			"description": "The statuses, types, priorities, audiences and standing lanes this repo's board runs on. Every sub-key is independently optional.",
			"properties": {
				"statuses": {
					"type": "object",
					"description": "The status role → label map. Every role is optional and falls to its shipped label.",
					"properties": {
						"needsTriage": {
							"type": "string",
							"minLength": 1
						},
						"triaged": {
							"type": "string",
							"minLength": 1
						},
						"needsInfo": {
							"type": "string",
							"minLength": 1
						},
						"planned": {
							"type": "string",
							"minLength": 1
						},
						"awaitingRelease": {
							"type": "string",
							"minLength": 1
						}
					},
					"additionalProperties": false
				},
				"types": {
					"type": "array",
					"description": "The `type:` labels triage may keep.",
					"items": {
						"type": "string",
						"minLength": 1
					},
					"uniqueItems": true,
					"minItems": 1
				},
				"priorities": {
					"type": "array",
					"description": "The priority labels triage may keep.",
					"items": {
						"type": "string",
						"minLength": 1
					},
					"uniqueItems": true,
					"minItems": 1
				},
				"audiences": {
					"type": "array",
					"description": "The audience labels triage may keep.",
					"items": {
						"type": "string",
						"minLength": 1
					},
					"uniqueItems": true,
					"minItems": 1
				},
				"standingLanes": {
					"type": "array",
					"description": "The standing-lane labels; an empty list means every issue homes on a milestone.",
					"items": {
						"type": "string",
						"minLength": 1
					},
					"uniqueItems": true
				}
			},
			"additionalProperties": false
		},
		"capClearAuthors": {
			"type": "array",
			"description": "Who may clear one extra repair round on a PR (`fabrika build clear`). Each entry is a GitHub `@user` or `@org/team`, `@`-prefixed. Empty (or absent) means nobody may grant.",
			"items": {
				"type": "string",
				"pattern": "^@([A-Za-z0-9](?:[A-Za-z0-9-]*[A-Za-z0-9])?)$|^@([^/\\s]+)\\/([^/\\s]+)$"
			}
		},
		"ci": {
			"type": "object",
			"description": "What fabrika may assume about the repo's continuous-integration producer.",
			"properties": {
				"noProducer": {
					"type": "string",
					"description": "What a repo with zero Actions workflows gets: `refuse` (a rollup over no producer is vacuous) or `degrade`.",
					"enum": [
						"refuse",
						"degrade"
					]
				},
				"gateWorkflow": {
					"type": "string",
					"description": "The CI gate's workflow filename, as `gh api actions/workflows/<file>` names it — a bare filename, not a path.",
					"minLength": 1,
					"pattern": "^[^/]+$"
				}
			},
			"additionalProperties": false
		},
		"codeValidators": {
			"type": "array",
			"description": "The repo's own commands that compile and lint its code — `build check --surface code` spawns each. Empty (or absent) refuses UNKNOWN rather than guessing a command line.",
			"items": {
				"type": "object",
				"properties": {
					"command": {
						"type": "array",
						"description": "The argv to spawn — e.g. [\"pnpm\", \"typecheck\"].",
						"items": {
							"type": "string"
						},
						"minItems": 1
					}
				},
				"required": [
					"command"
				],
				"additionalProperties": false
			}
		},
		"containmentVocabulary": {
			"type": "object",
			"description": "Which child types are asked for a `**Containment:**` marker, and which values satisfy it. An empty half means the marker is never required.",
			"properties": {
				"types": {
					"type": "array",
					"description": "The `type:` labels a child must carry to be asked for a marker.",
					"items": {
						"type": "string",
						"minLength": 1
					}
				},
				"values": {
					"type": "array",
					"description": "The keywords that satisfy the marker. Never \"none\", the reserved declination.",
					"items": {
						"type": "string",
						"minLength": 1
					}
				}
			},
			"additionalProperties": false
		},
		"cycleDoc": {
			"type": "string",
			"description": "The product-development cycle doc a containment class is derived from, repo-relative.",
			"minLength": 1,
			"pattern": "^(?!/)(?!\\.\\.).+"
		},
		"decisionsDir": {
			"type": [
				"string",
				"null"
			],
			"description": "The decision corpus directory, repo-relative. Write null to declare that this repo keeps no decision corpus.",
			"minLength": 1,
			"pattern": "^(?!/)(?!\\.\\.).+"
		},
		"designHarness": {
			"type": "string",
			"description": "The design-harness file declaring the dev server `ui render` renders at, repo-relative.",
			"minLength": 1,
			"pattern": "^(?!/)(?!\\.\\.).+"
		},
		"docLeakExempt": {
			"type": "array",
			"description": "Docs whose subject IS path hygiene — `build check --surface prose` skips its leak scan on them. Repo-relative path suffixes. Empty (or absent) means nothing is exempt.",
			"items": {
				"type": "string",
				"minLength": 1
			}
		},
		"governedRoots": {
			"type": "array",
			"description": "The repo-relative roots a diff derives the governance namespace over. Must cover .fabrika.jsonc itself — a config cannot un-govern itself.",
			"items": {
				"type": "string",
				"minLength": 1
			},
			"minItems": 1
		},
		"roadmapFile": {
			"type": "string",
			"description": "The roadmap file carrying the `## Campaigns` focus table, repo-relative.",
			"minLength": 1,
			"pattern": "^(?!/)(?!\\.\\.).+"
		},
		"surfaceDispositions": {
			"type": "object",
			"description": "What this repo does when a surface fabrika reads is missing. Declare only the ids you move; every other falls to its shipped disposition.",
			"propertyNames": {
				"enum": [
					"gh-rest",
					"git-worktree",
					"git-origin",
					"git-history",
					"collaborator-permissions",
					"issue-dependencies",
					"label-taxonomy",
					"issue-shape-markers",
					"standing-lanes",
					"issue-home",
					"roadmap-focus",
					"cycle-doc",
					"decisions-corpus",
					"decisions-rarity-floor",
					"patterns-dir",
					"patterns-index",
					"patterns-admission-bar",
					"glossary-registers",
					"prose-homes",
					"doc-gates",
					"dep-manifest",
					"design-manifest",
					"design-prohibitions",
					"design-inventory",
					"golden-pointer",
					"design-harness",
					"dev-server",
					"preview-deployment",
					"codeowners",
					"landing-path",
					"required-status-contexts",
					"governance-floor-workflow",
					"code-validators",
					"readout-artifact",
					"gitignore-row",
					"temp-root",
					"claim-signal",
					"verdict-history"
				]
			},
			"additionalProperties": {
				"type": "string",
				"enum": [
					"fail-loud",
					"degrade",
					"bootstrap"
				]
			}
		},
		"triageFacets": {
			"type": "array",
			"description": "What each triage facet owns and every label an input can make it keep. A facet declares its ownership as `owns` (a regex) or `ownsLabels` (an explicit set), never both.",
			"minItems": 1,
			"items": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"minLength": 1,
						"description": "The facet's name."
					},
					"values": {
						"type": "array",
						"description": "Every label this facet may keep.",
						"items": {
							"type": "string",
							"minLength": 1
						},
						"minItems": 1
					},
					"owns": {
						"type": "string",
						"minLength": 1,
						"description": "A regular expression matching the labels this facet has delete authority over."
					},
					"ownsLabels": {
						"type": "array",
						"description": "The explicit set of labels this facet has delete authority over.",
						"items": {
							"type": "string",
							"minLength": 1
						},
						"minItems": 1
					}
				},
				"required": [
					"name",
					"values"
				],
				"additionalProperties": false
			}
		},
		"unreadableCodeowners": {
			"type": "string",
			"description": "What a §CP read does when `.github/CODEOWNERS` itself cannot be read: `ship` treats the boundary as absent, `refuse` raises the caller's UNKNOWN. Nothing reads this key today.",
			"enum": [
				"ship",
				"refuse"
			]
		},
		"workflowValidators": {
			"type": "array",
			"description": "The repo's own commands that machine-read `.github/workflows/**` — `build check --surface workflows` spawns each. Empty (or absent) stands on actionlint alone.",
			"items": {
				"type": "object",
				"properties": {
					"command": {
						"type": "array",
						"description": "The argv to spawn — e.g. [\"node\", \"tools/lint-workflows.js\"].",
						"items": {
							"type": "string"
						},
						"minItems": 1
					},
					"reads": {
						"type": "array",
						"description": "The exact workflow files this command opens — what makes a passing run checkable per file.",
						"items": {
							"type": "string",
							"minLength": 1
						},
						"minItems": 1
					}
				},
				"required": [
					"command",
					"reads"
				],
				"additionalProperties": false
			}
		}
	},
	"additionalProperties": false
}
