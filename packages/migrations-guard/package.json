{
	"name": "@kampus/migrations-guard",
	"version": "0.0.0",
	"private": true,
	"description": "Fail-closed CI gate over the committed D1 migrations tree (ADR 0309): consistency (frozen flat history, no .sql at any depth outside the two known layouts, snapshot.json per directory, unique apply prefixes), ordering (contiguous flat numbers, directories sorting after them), and immutability (a landed migration's SQL never changes vs a committed baseline) — issue #1435",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json && effect-tsgo diagnostics --project tsconfig.json --strict",
		"test": "vitest run",
		"check": "node src/bin.ts check",
		"baseline": "node src/bin.ts baseline"
	},
	"dependencies": {
		"@effect/platform-node": "catalog:",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@effect/tsgo": "catalog:",
		"@effect/vitest": "catalog:",
		"@types/node": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	},
	"volta": {
		"node": "26.2.0"
	}
}
