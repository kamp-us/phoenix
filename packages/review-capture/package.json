{
	"name": "@kampus/review-capture",
	"version": "0.0.0",
	"private": true,
	"description": "Playwright-capture + GitHub user-attachments upload helper for the review-design gate (ADR 0165, epic #1966) — shoots a UI PR's changed surfaces over its per-PR preview deploy and hosts the PNGs as GitHub attachments for the SHA-bound verdict",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsgo -p tsconfig.json",
		"test": "vitest run --project unit",
		"test:unit": "vitest run --project unit",
		"capture": "node src/bin.ts run"
	},
	"dependencies": {
		"@effect/platform-node": "catalog:",
		"@playwright/test": "catalog:",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@effect/tsgo": "catalog:",
		"@effect/vitest": "catalog:",
		"@types/node": "catalog:",
		"@typescript/native-preview": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
