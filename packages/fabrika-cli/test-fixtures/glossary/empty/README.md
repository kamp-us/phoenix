This directory is deliberately empty of registers.

It exists so the `glossary drift` and `glossary sections` contract examples have a readable
directory that holds no register file — the `bootstrap` case. Git does not track an empty
directory, so this file is what keeps the example reproducible after a clone.
