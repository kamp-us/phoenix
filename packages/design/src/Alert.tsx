import "./Alert.css";

/**
 * @component Alert
 * @whenToUse A Manti-backed inline semantic message. Use `danger` for errors and
 *   another semantic variant for non-error outcomes; add `kp-alert--inline` when
 *   preserving a feature's existing text-only geometry.
 * @slot children The message description.
 */
export type {AlertProps, AlertVariant} from "@manti-ui/react";
export {Alert} from "@manti-ui/react";
