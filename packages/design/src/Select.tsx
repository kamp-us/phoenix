/**
 * @component Select
 * @whenToUse The Manti-backed selection field for a compact fixed option list
 *   whose choices need keyboard navigation and a styled listbox.
 * @slot items The options supplied through `items`.
 */
export type {SelectItem, SelectProps, SelectVariant} from "@manti-ui/react";
export {Select} from "@manti-ui/react";
