{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"noEmit": true,
		// The package ships TS source (`exports` → `./src/index.ts`); consumers
		// bundle it. Repo style imports with explicit `.ts` specifiers.
		"allowImportingTsExtensions": true,
		"types": ["node"]
	},
	"include": ["src", "vitest.config.ts"]
}
