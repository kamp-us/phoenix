{
	"name": "@kampus/db-schema",
	"version": "0.0.0",
	"private": true,
	"description": "Single canonical Drizzle declaration of the shared D1 tables (term_record / definition_record / post_record / comment_record) — the one source the worker, preview-seed, and fts-backfill all import so a column rename is one edit (issue #859)",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json && effect-tsgo diagnostics --project tsconfig.json --strict",
		"test": "vitest run"
	},
	"dependencies": {
		"drizzle-orm": "catalog:"
	},
	"devDependencies": {
		"@effect/tsgo": "catalog:",
		"@types/node": "catalog:",
		"effect": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	},
	"volta": {
		"node": "26.2.0"
	}
}
