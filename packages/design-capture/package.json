{
	"name": "@kampus/design-capture",
	"version": "0.0.0",
	"private": true,
	"description": "phoenix's per-repo half of the golden-screen loop (ADR 0183): the depo store/fetch boundary for golden bytes, the committed golden-pointer.json, and the CLI the review-design gate drives. The portable capture/render/diff machinery is @kampus/fabrika-cli/capture (#5063).",
	"type": "module",
	"exports": {
		".": "./src/golden-store.ts"
	},
	"scripts": {
		"typecheck": "tsgo -p tsconfig.json",
		"capture": "node src/bin.ts capture",
		"golden-bless": "node src/bin.ts golden-bless"
	},
	"dependencies": {
		"@effect/platform-node": "catalog:",
		"@kampus/depo": "workspace:*",
		"@kampus/fabrika-cli": "workspace:*",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@effect/tsgo": "catalog:",
		"@types/node": "catalog:",
		"@typescript/native-preview": "catalog:",
		"typescript": "catalog:"
	}
}
